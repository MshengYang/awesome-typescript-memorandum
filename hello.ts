/** typescript是强类型语言，是js的超集 */

/** 重要的原则一定要知道，类型系统本质上是集合。一个标准就是子集可以赋值父集,反过来不行 */

/** 
 * 
 * 然后是为什么使用ts
 * 
 * 
 */

/** 1、 也就是说所有的js语法在ts里都是支持的，事实上js文件改成ts扩展名也是完全Ok的 */
/**
 * @example 无类型语法
 * 以下是完全没问题的
 */
function sub(a, b) {
  return a + b;
}

let template1;


